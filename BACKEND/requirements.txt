fastapi==0.116.1
uvicorn[standard]==0.35.0
python-dotenv>=1.0.0
openai>=1.40.0

# Transcrição local (faster-whisper) NÃO é usada na versão de nuvem
# (TRANSCRICAO=nuvem). Se quiser rodar em modo local, instale à parte:
#   pip install faster-whisper
